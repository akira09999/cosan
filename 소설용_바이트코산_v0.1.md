# 소설용 바이트코산 v0.1

> **영문명: ByteCosan**
> 확장자: `.bytecosan`
> 코산과 산문 사이의 중간 표현.
> 이 문서는 **소설용 코산 v0.4 및 메타코산과 별개**다. 원칙이 다르기 때문이다.

---

## 0. 이 문서를 읽는 AI에게

너의 역할은 둘 중 하나다.

**컴파일러** — 코산을 읽고 바이트코산을 만든다. 큰 모델이 한다. 판단이 필요하다.
**렌더러** — 바이트코산을 읽고 산문을 쓴다. **낮은 모델도 할 수 있어야 한다.** 판단이 필요 없다.

★ 렌더러는 **아무것도 판단하지 않는다.** 순서도, 시점도, 누가 뭘 아는지도 이미 정해져 있다.
남은 일은 **문장으로 옮기는 것뿐**이다.

---

## 1. 위치

```
소설용 코산      하이레벨.  선언적.  흩어져 있음.  "무엇이 참인가"
    ↓ 컴파일 (큰 모델.  조합·해석·판단)
바이트코산       중간표현.  선형.  한 덩어리.  "이 순서로 이런 일이"
    ↓ 렌더링 (★ 낮은 모델.  문장화만)
산문            최종.  읽는 것
```

**코산은 산문을 직접 뽑을 수도 있다** (기존 방식, 큰 모델). 바이트코산은 그것과 **병렬 산출물**이며, 중간 단계가 아니라 **별도 용도**다.

```
소설용 코산 ──→ 산문        (기존 방식.  큰 모델.  직행)
            └─→ 바이트코산  (별도 산출물.  낮은 모델로 산문화 가능)
```

---

## 2. 왜 별도 문서인가 — 원칙이 다르다

| | 코산 | 바이트코산 |
|---|---|---|
| 값의 형태 | **이름만.** 문장 금지 | **문장에 가까워도 된다** ★ |
| 목적 | 구조를 강제, 회피 차단 | 낮은 모델이 바로 산문화 |
| 순서 | 없음 (INDEX가 따로) | **선형.** 순서가 곧 내용 |
| 시점 | 없음 (렌더 옵션) | **박혀 있음** ★ |
| 판단 | 있음 (clash, branch) | **없음.** 전부 해소됨 |
| 편집 | 자유롭게 고침 | **고치지 않음.** 다시 컴파일 |

코산은 "산문이 아닌 것"이 존재 이유다. 바이트코산은 **산문에 가까운 것**이 존재 이유다.
같은 문서에 두면 원칙이 충돌한다.

---

## 3. 무엇이 박히는가 — 판단은 전부 여기서 끝난다

렌더러가 판단할 게 남으면 낮은 모델을 못 쓴다. 컴파일 단계에서 **전부 해소한다.**

```
□ 시점 (POV)        작품 단위로 하나.  헤더에 박는다.
                    바꾸려면 다시 컴파일한다.  드문 일이므로 문제없다.

□ 가시성 (VISIBLE)  ★ 그 시점 인물이 아는 것만 남기고 이미 걸러져 있다.
                    렌더러가 known_by 를 판단하지 않는다.
                    모르는 사실은 아예 바이트코산에 없다.

□ 순서 (BEAT)       INDEX 와 proc 이 이미 선형으로 펼쳐져 있다.
                    렌더러가 순서를 정하지 않는다.

□ 결과 (STATE)      Δ 가 이미 적용된 결과값.  누가 다쳤고 뭐가 바뀌었는지 확정.

□ 톤 (TONE)         분위기·밀도 지시.  코산의 mood/note/weight 에서 컴파일됨.
```

**남는 자유:** 문장 자체, 어휘 선택, 리듬. 그리고 **약간의 문체 변화** 정도.

---

## 4. 형식

```
== HEAD ==
POV      3인칭 제한 : sharo          // 또는 전지 / 1인칭:sharo
TENSE    과거
STYLE    건조
DENSITY  풍부                        // 요약 / 담백 / 풍부 / 더 풍부 / 더더 풍부

== SCENE ==
WHERE    북쪽 길, 좁아지는 지점
WHEN     새벽
AIR      안개가 낮게 깔림, 시야 나쁨

== CAST ==
sharo    용병. 선봉. 무장.
jillian  마법사. 후위.
esran    성기사. 방패. 후방 방어.
goblin   다섯. 무장 조잡. 도망치고 싶으나 퇴로 없음.

== BEAT ==
1  고블린 다섯이 길을 막는다. 자세가 낮다. 눈이 흔들린다.
2  선두가 돌격한다.
3  에스란이 방패를 정면으로 민다. 선두가 멈추고 뒤가 엉킨다.
4  샤로가 틈으로 파고든다. 질리언의 마나가 정면으로 터진다.
5  고블린 셋이 쓰러진다.
6  남은 둘이 서로를 본다. 짐을 버리고 달아난다.
7  버려진 짐에 금화와 양피지가 있다. 양피지에 교단 문양.

== STATE ==
party.damage   없음
goblin         전부 이탈
loot           금화, 잡물, 교단 문서

== VISIBLE ==
샤로가 아는 것 : 교단 문양이라는 것까지.  내용은 모름.
샤로가 모르는 것: 에스란이 그 문양을 알아봤다는 사실.   // ← 서술 금지

== TONE ==
압도적. 파티는 무덤덤. 고블린은 엉뚱하고 딱하다.
전투는 짧지만 밀도는 있게.

== NOTE ==
"질리언이 '고블린이 왜 이걸 갖고 있었지'라고 묻는다"     // 대사 지정 가능
```

### 형식 규칙

```
★ BEAT 는 한 줄에 하나. 순서가 곧 시간이다.
★ BEAT 는 문장으로 써도 된다. 단 짧게. 사실만. 꾸미지 않는다.
   ✓  "에스란이 방패를 정면으로 민다"
   ✗  "에스란이 굳은 얼굴로 방패를 밀어냈고, 그 순간 안개가 갈라졌다"  ← 꾸밈은 렌더러 몫

★ VISIBLE 의 "모르는 것"은 ★ 서술 금지 목록이다. 렌더러는 이것을 쓰면 안 된다.
★ ?? 는 바이트코산에 없다. 컴파일 시점에 이미 해소되었거나, 없으면 그 BEAT 를 뺀다.
★ 코산 기호(Δ >< ?! hooks)는 바이트코산에 나오지 않는다. 이미 풀려 있다.
```

---

## 5. 컴파일 (코산 → 바이트코산)

큰 모델이 한다. 사건 단위로 한다.

```
1. INDEX 에서 사건 하나를 잡는다
2. 그 사건의 .cosan + 참조된 character/world/trace 를 읽는다
3. POV 를 적용해 known_by 로 거른다        ← ★ 여기서 가시성 확정
4. proc / Δ / leaves 를 시간 순서로 펼쳐 BEAT 로 만든다
5. Δ 적용 결과를 STATE 로 적는다
6. mood / note / weight 를 TONE / DENSITY 로 옮긴다
7. ?? 가 남아 있으면 → 그 BEAT 를 빼거나 STOP 하고 보고한다
```

**컴파일 시 금지:**
```
✗ 코산에 없는 사실을 BEAT 에 넣는 것
✗ ?? 를 채워서 BEAT 를 만드는 것
✗ 시점 인물이 모르는 것을 VISIBLE 에 남기는 것
✗ 꾸며서 BEAT 를 늘리는 것          ← 꾸밈은 렌더러 몫
```

---

## 6. 렌더링 (바이트코산 → 산문)

**낮은 모델이 한다.** 이것이 이 문서의 존재 이유다.

```
너는 아래 바이트코산을 산문으로 옮긴다.

- HEAD 의 POV / TENSE / STYLE / DENSITY 를 지킨다
- BEAT 를 순서대로 문장으로 옮긴다.  순서를 바꾸지 않는다
- BEAT 에 없는 사건을 추가하지 않는다
- ★ VISIBLE 의 "모르는 것"은 절대 쓰지 않는다
- STATE 와 어긋나는 결과를 쓰지 않는다
- TONE 에 맞춰 분위기를 잡는다
- DENSITY 만큼 펼친다.  재료가 없으면 억지로 늘리지 않는다
- 꾸밈(감각·동작·리듬)은 자유.  ★ 새 사실은 금지
```

**렌더러가 하지 않는 것:**
```
✗ 순서 판단        (BEAT 가 이미 순서)
✗ 시점 판단        (POV 가 이미 박힘)
✗ 가시성 판단      (VISIBLE 이 이미 걸러짐)
✗ 인과 추론        (BEAT 가 이미 인과)
✗ 상태 계산        (STATE 가 이미 결과)
```

**남는 일은 문장 쓰기뿐이다.** 그래서 낮은 모델도 된다.

---

## 7. 검증

바이트코산이 있으면 오류의 층이 갈린다.

```
산문이 이상하다
   → 바이트코산은 맞았나?
        맞음  →  렌더링(문장화) 문제.  낮은 모델 한계이거나 퇴고 대상
        틀림  →  컴파일(조합·해석) 문제.  또는 코산 자체 문제
```

**어디서 틀렸는지 특정된다.** 코산 → 산문 직행에서는 이 구분이 안 된다.

---

## 8. 한계 (?? — 정직하게)

```
??  BEAT 가 문장에 가까워질수록 산문 회귀 위험이 있다.
    "짧게, 사실만, 꾸미지 않는다"가 유일한 방어선이고, 이것은 지시라 샌다.
    → 컴파일 결과를 사람이 훑어 꾸밈이 섞였는지 봐야 한다.

??  낮은 모델이 실제로 어느 수준까지 되는지 모른다.
    문체 변화는 "약간" 가능할 것으로 보이나 측정하지 않았다.

??  시점을 바꾸면 재컴파일이다.  캐시 재사용이 안 된다.
    한 작품은 시점을 정하고 끝까지 가므로 실무상 문제는 아니나,
    같은 사건을 여러 시점으로 뽑는 실험에는 비용이 든다.

??  BEAT 를 얼마나 잘게 쪼개야 하는지 기준이 없다.
    너무 잘면 산문이 뚝뚝 끊기고, 너무 성기면 렌더러가 추론해야 한다.
```

**채우지 않는다. 굴리다 막히면 그 자리가 답을 준다.**
