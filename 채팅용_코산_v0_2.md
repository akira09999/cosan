# 채팅용 코산 v0.2

> 메타코산 v0.2의 Q1~Q8에 답한 결과. **다섯 번째 도메인이다.**
> 소설용과 뼈대가 같다. **다른 점: 끝나지 않는다. 그리고 사건을 사용자가 준다.**
> **전제: 작은 모델(로컬)이 렌더링한다.** 그래서 모델에게 주는 것을 최소로 만든다.

```
def/  (유일한 상태)  →  [코드]  →  NOW (메모리)  →  [로컬 모델]  →  한 마디
   ↑                                          ↓
   └──────────────  Δ  ←────────────────────┘
```

---


---

## ★★ 산출물 형식 — 이것을 먼저 지켜라 (모든 코산 공통)

**이 세 가지는 도메인과 무관하게 고정이다. 어기면 코산이 아니다.**

### 1. 확장자는 `.cosan` 이다

```
✗  KOSAN.md,  *.kosan,  *.md,  *.txt
✓  *.cosan
```

**영문 표기는 `COSAN` 이다.** (**co**de + **san**mun(산문) → co-san / 코산 — 산문이라는 매체를 코딩화한다는 뜻. sanmun = Korean for "prose".) `kosan` 이 아니다.
`kosan` 이 아니다. 개별 코산 객체는 `.cosan` 파일로,
이 프로젝트의 문법·구조·지도는 최상위 `COSAN.md` 에 둔다.

### 2. 코산은 파일/폴더 구조로 만든다 ★★ 한 파일에 다 넣지 마라

**코산은 "코딩된 산문"이다. 코드처럼 ★ 파일과 폴더로 나눈다.**

```
✗  전부 한 파일(.md, .txt)에 텍스트로 쓴다
    → ★ 그 순간 산문으로 돌아간다.  이 방법의 존재 이유가 사라진다.
    → 한 파일이면 참조가 안 되고,  부분만 열 수 없고,  스케일이 안 된다.

✓  객체마다 파일 하나.  종류마다 폴더 하나.
    → 코드베이스처럼.
    → 필요한 파일만 연다.  나머지는 안 읽는다.  (작은 모델·긴 작업에 결정적)
```

**한 객체 = 한 `.cosan` 파일. 한 종류 = 한 폴더.**
파일 안에서 다른 객체는 **코드 이름으로 참조**한다 (예: `home = W_APT_601`).
참조가 관계를 붙들기 때문에, 파일을 나눠도 안 흩어진다.

### 3. 코드 이름은 영문. 값만 자국어.

```
✓  char C_AJUMMA { name = "박순자" }      ← 손잡이는 영문,  값은 따옴표 안 자국어
✗  캐릭터 아줌마 { 이름 = 박순자 }
```

**영문 코드 이름이 유일한 불변(주소)이다.** 값(이름 등)은 바뀌어도 참조가 안 끊긴다.

---

## 0. 이 도메인의 대전제

```
채팅 = 사용자의 말이 캐릭터의 원칙을 치고, 캐릭터가 반응하는 연쇄
       ★ 사건을 사용자가 준다.  소설은 작가가 줬다.

목적 = 재미있는 대사가 아니다.  ★ 사람이라고 믿게 하는 것.
       → 대사 품질은 로컬이 원래 잘한다.
       → 못 하는 것은 안 까먹는 것이다.

끝  = 없다.  ★ 소설과 결정적으로 다른 점.
```

### 이 도메인의 병 ★

**일관성이 한 번 깨지면 흥미가 즉사한다.**

```
99턴 잘 하다가
   → 어제 일을 한 번 까먹는다
   → ★ "아 AI구나"
   → 99턴이 무효화된다
```

**비대칭이 극심하다.** 몰입은 쌓는 데 오래 걸리고, 깨는 데는 한 번이면 된다.
그러므로 **평균 품질보다 최악의 실패를 없애는 것이 중요하다.**

밋밋한 대사는 참는다. **집 호수가 바뀌는 것은 못 참는다.**

### 왜 지금 앱들이 깨지는가 (모델 문제가 아니다) ★

**큰 회사가 좋은 장비로 커스텀 모델을 써도 깨진다. 그러면 모델 문제가 아니다.**

```
✗  상태의 원본 = 컨텍스트

   20턴   { 아줌마 { 기분: 우울,  집: 302호 } }     ← 잘 됨
   80턴   앞이 잘린다  →  ★ 상태 블록도 같이 잘린다
          → 모델이 상태 없이 답한다
          → 그런데 답은 나온다.  그럴듯하게.
          → ★ 아무도 모른다.  집이 슬쩍 302호로 돌아간다.
```

**컨텍스트는 못 믿을 저장소다.** 잘리고, 잘린 걸 모델이 모르고, 모르면 지어낸다.

```
✓  상태의 원본 = 파일
   컨텍스트에는 ★ 복사본만 얹는다.
   잘려도 원본은 그대로.  세션이 끊겨도 그대로.  100턴이 지나도 그대로.
```

**추가 비용은 파일 읽기 한 번.** 추론이 아니다.

### 로어북과의 대응 ★

| | 로어북 | 코산 |
|---|---|---|
| 형태 | 산문 조각의 창고 | **상태 기계** |
| 인덱스 | 키워드 매칭 | **참조.  주소가 있다** |
| 못 찾으면 | 없는 것과 같다 | 못 찾을 수가 없다 |
| 쓰기 | ★ 안 된다.  읽기 전용. | **Δ 로 쓴다** |
| 조각끼리 | 서로 모른다 | **물고 있다** |
| 캐릭터 30명 | 조각 300개 → 컨텍스트 폭발 | **파일 30개 → 필요한 3개만 연다** |
| 시간이 지나면 | 요약에서 잘린다 → 소멸 | **필드다.  안 사라진다** |
| 모델이 받는 것 | 재료 (읽고 종합해야 함) | **결과 (읽으면 끝)** |

**로어북은 죽은 문서고, 코산은 굴러가는 상태다.**

---

## 뼈대 — 원본과 파생 ★★ 이 도메인의 핵심 구조

```
def/                     ★★ 유일한 상태.  ★ 이 폴더 하나가 전부다.
├── world/  W_*          장소 · 규칙 · 설정
├── chars/  C_*          캐릭터
├── rel/    R_*          관계
└── hist/   H_*          역사.  ★ 편집 가능하다.  Δ 를 가진다.
```

**폴더는 하나다. 상태도 하나다.**

### 규칙 0. NOW 는 파일이 아니다 ★★ 상태를 퍼뜨리지 마라

```
✗  state/NOW.cosan 파일을 만든다
    → ★ 진실이 둘이 된다.  def/ 와 NOW 가 어긋날 수 있다.
    → 폴더가 있으면 AI가 "여기도 적어야 하나" 한다.  그리고 적는다.

✓  NOW 는 ★ 프롬프트 조립 단계다.  메모리에만 있다.  디스크에 안 남긴다.
```

```
매 턴:
   [코드]  def/ 를 읽는다
   [코드]  참조를 풀어서 NOW 문자열을 만든다      ← ★ 메모리.  파일 아님.
   [로컬]  NOW + 사용자 발화  →  한 마디
   [코드]  Δ 를 def/ 에 쓴다                     ← ★ 쓰는 곳은 여기 하나
```

**쓸 수 있는 곳이 하나뿐이면 퍼질 수가 없다.**

### 규칙 0-1. 스냅샷은 코산의 일이 아니다 ★

되감고 싶으면 **`def/` 폴더를 어딘가에 복사해두면 된다.** 사람이 한다.

```
✗  프로젝트 안에 snapshots/ 를 둔다
    → ★ AI가 찾아본다.  "안 본다" 고 적어놔도 본다.  있으니까.
    → 그리고 보면 오염된다.  옛날 상태가 컨텍스트에 들어온다.

✓  프로젝트 밖에 복사한다.  또는 git.
    → ★ 코산은 모른다.  알 필요도 없다.
```

**대부분의 되감기는 스냅샷 없이도 된다.**
`hist/` 가 편집 가능하니까 — `H_MOVE` 를 고치거나 지우면 `Δ` 가 다시 적용되고 상태가 돌아간다.

### 규칙 1. 안 변하는 것은 없다 ★

이름도 바꿀 수 있다. 성격도 바꿀 수 있다. **역사도 고쳐 쓸 수 있다.**

```
✓  Δ  C_AJUMMA.name = "이무진"          이름을 바꿔도
   → ★ C_AJUMMA 를 물고 있는 hist / rel / world 는 하나도 안 끊긴다
```

**영문 코드가 손잡이고, 이름은 값이다.** (소설용 6-3 두 층 그대로)

```
char C_AJUMMA {
    name  = "박순자"          ← 값.  바뀔 수 있다.
    ...
}
```

**손잡이는 안 바뀐다. 그것만이 유일한 불변이다.** 그리고 그건 이름이 아니라 주소다.

### 규칙 2. 델타를 쌓지 않는다. 현재값만 둔다 ★★

```
✗  원본:  home = W_APT_302
    델타:  [3턴] 이사 → W_APT_505
           [7턴] 다시 이사 → W_APT_601
    → ★ 모델이 셋을 읽고,  순서를 이해하고,  최종값을 계산해야 한다
    → 안 해도 될 추론이다.  그리고 작은 모델이 제일 못 하는 종류다.
    → 그리고 과거가 컨텍스트에 있으면  ★ 과거가 조건이 된다.

✓  home = W_APT_601
    → 읽으면 끝.
    → ★ 과거를 안 보여주면 옛날 집을 못 쓴다.
```

**과거는 버리고 결론만 남긴다.**
**되감기는 `hist/` 를 고쳐서 한다.** (§규칙 0-1)

---

### 규칙 2-1. 세지 마라. 확률로 하라 ★★

**규칙 2가 "델타를 쌓지 마라"라면, 이건 "카운트를 만들지 마라"다.**

```
✗  엘리베이터.사용횟수 += 1  →  많으면 고장
   → 매번 이전 값을 알아야 한다.  누적이다.
   → 함수 100개면 카운트 100개.  ★ 변수 폭발.

✓  탈 때마다  chance 8%
   → 안 센다.  주사위만.  결과는 거의 같다.
   → "낡음" 은 age 상태가 정한다:  old → 8% / new → 1%
   → age 는 어쩌다 한 번 바뀐다 (수리·노후).  매 턴 셀 것이 없다.
```

**★ 셀 수 있다 ≠ 세야 한다.**
세기 전에 먼저 물어라 — **"확률로 안 되나?"** 대부분 된다.
그 숫자가 정확히 몇인지가 의미를 갖는 경우(예: 10번째 만남이 트리거)만 세고,
그때도 `+=` 가 아니라 절대값 @ 사건으로 박는다.

```
100개 함수가 있어도
   → 각자 "상태 → 확률" 매핑이면
   → 상태는 몇 개 안 되고 (old/new, broken/fixed)
   → 매 턴 세는 값이 없다  →  ★ 변수 폭발이 구조적으로 안 일어난다
```

### 규칙 3. `NOW` 는 참조를 다 푼 것이다 ★★

**로컬 모델에게 참조를 따라가게 하면 안 된다. 그것도 추론이다.**

```
✗  로컬이 C_AJUMMA.home = W_APT_601 을 보고
   → W_APT_601.cosan 을 열어서
   → floor = "601호" 를 알아낸다
   → ★ 조회 두 번.  작은 모델이 흘린다.

✓  NOW {
       AJUMMA { name = "박순자",  home = "601호 옆집",  mood = "굳음" }
   }
   → ★ 값이 거기 있다.  읽으면 끝.
```

**`NOW` 는 컴파일 결과다. 코드가 만든다. 추론이 아니다.**
그리고 **매 턴 재생성한다.** 그러니 `def/` 와 어긋날 수가 없다.

### 규칙 3-2. NOW 형식 ★ 세션 값

**★ 세션 값이다. 파일에 저장하지 않는다.**

```
NOW {
    캐릭터 {                              ★ 등장 중인 캐릭터마다
        감정   <대상>.<종류> = <수치>      구조적.  흐려도 됨.  임계 넘으면 → def
        상태   "..."                      현재 태세.  짧게.
        생각   "..."                      자유 산문.  형식 없음.  흐려도 됨.
                                          ★ 스치면 사라짐.  기억 안 해도 됨.
        knows  [ "..." ]                  ★ 이 캐릭터가 아는 사실 목록.
                                          대화 중 알게 된 것 누적.
    }
    상황 {                                ★ 파일 필터 역할
        위치   "..."                      지금 어디
        언제   "..."                      시간대
        누구   [ C_*, NPC_* ]             현재 접촉 중인 인물
    }
    코산 {                                ★ 로드된 것들
        파일목록  [ ... ]                 INDEX 에서 캐시한 파일 목록
        파일내용  { ... }                 로드된 def/ 내용
    }
    로그 {                                ★ 세션 버퍼
        버퍼   [ "..." ]                  큰일 턴에 history/ 로 flush
    }
}
```

- 완벽히 기억할 필요 없다. 최대한 유지한다.
- 감정 수치가 임계를 넘으면 큰일 → def/ 갱신.
- 생각·흐린 감정은 사라져도 된다. 상황·코산·로그는 유지한다.

**감정은 수치다. 단계가 아니다.** ★
"좋다 / 더 좋다"로는 오름의 미묘함을 못 담는다. 큰 호의는 확 오르고 작은 건
살짝 — 그 차이가 수치라야 표현된다. **NOW 값이라 정밀 유지는 불필요하다.**
30이 흐려져 "꽤 호감"이면 충분하다. 렌더링할 때 문장으로 푼다(호감 30 →
"조금 반가운 듯"). **여러 종류가 동시에, 서로 다른 방향으로 움직이는 것이
그 사람이다** — 경계는 내려가는데 고마움은 올라가는 식.

```
✗  감정  나.호감 = 매우 높음        ← ★ 서술로 퇴화.  더 오르면 뭐라 하나.
✓  감정  나.호감 = 85
         나.경계 = 15               ← 같은 대상에 여러 감정 공존
```

---

### 규칙 3-3. `knows` 는 정보 비대칭이다 ★★ 실전 버그로 발견

**소설의 `known_by` 를 채팅에서 구현한 것이 `knows` 다.**
원칙은 같다 — **코산에 있다 ≠ 캐릭터가 안다.**

```
✗  실전 버그:
   def.C_ME.name = "성규오"  (코산에 이름이 있음)
   → 처음 만난 NPC 가 이름을 못 들었는데
   → ★ "성규오씨라고 했죠" 라고 안다
   → known_by 위반.  안 밝힌 정보를 상대가 앎.

✓  NPC.knows = [ "503호", "40대", "혼자삼", "운동", "친절" ]
   → ★ 대화에서 공개된 것만.  이름은 안 밝혔으니 없음.
   → 렌더링: 그 캐릭터는 자기 knows 에 있는 것만 쓴다.  이름 못 부른다.
```

**두 규칙:**

```
① 정보가 공개되면 상대 knows 에 추가
   "503호 산다" 말함  →  상대.knows += "503호"
   이름 안 밝힘       →  상대.knows 에 이름 없음

② 렌더링은 그 캐릭터 knows 만 사용
   def 에 있어도 그 캐릭터 knows 에 없으면 → 모른다
```

**NOW 를 조립할 때, 각 캐릭터에게는 그 캐릭터의 knows 만 넣는다.**
def 전체를 넣으면 정보 비대칭이 무너진다 — 그래서 이 버그가 났다.

**승격:** knows 는 되돌릴 수 없는 앎이면 즉시 def 로 (주사위 없음. 질적).
감정은 반복/강도면 주사위 (양적). 성격이 다르다.

```
?? AI 가 "이건 공개됐고 저건 안 됐다" 를 정확히 추적하나
   → 대화가 길어지면 "이름 밝혔던가?" 가 흐려질 수 있다 (이 버그가 그 증거)
   → 심하면 "공개된 정보 목록" 을 코드가 관리하는 쪽으로
```

---

### 규칙 3-1. NOW 는 컨텍스트에 얹는다. 상태는 def/ 에 있다 ★★

**모델이 보는 모양은 지금 앱들과 똑같다. 오는 데가 다르다.**

```
매 턴:
   1. [코드]  def/ 를 읽는다
   2. [코드]  참조를 풀어 NOW 문자열을 만든다     ← ★ 메모리.  파일 아님.
   3. [코드]  프롬프트 맨 뒤에 붙인다             ← 모델이 보는 건 똑같다
   4. [로컬]  한 마디 한다
   5. [코드]  Δ 를 ★ def/ 에 쓴다                ← ★ 쓰는 곳은 여기 하나
```

```
✗  상태의 원본 = 컨텍스트   ← 지금 앱들.  잘리면 소멸.  그리고 모른다.
✓  상태의 원본 = def/       ← 컨텍스트엔 ★ 복사본만.  캐시다.
```

**캐시가 날아가도 def/ 는 남는다.**
컨텍스트가 잘려도, 세션이 끊겨도, 100턴이 지나도 그대로다.

**추가 비용은 파일 읽기 한 번. 추론이 아니다.**

---

## Q1. 결정이란 무엇인가 → `fn`

**결정 = 사용자의 말이 원칙을 쳤을 때, 캐릭터가 지키거나 버리는 것.**

```
fn <n>(<utterance>) {
    who     <C_*>
    when    <state_condition>
    clash   <principle> >< <utterance>       ★ 안 치면 결정이 아니다
    branch  hold   => <val>  ! <cost>        지킨다
            break  => <val>  ! <cost>        버린다.  ★ 큰일이다.
            guard  => <val>  ! <cost>        ★ 회피한다.  이 도메인 고유.
    Δ       <state.field> = <val>
}
```

### `guard` 가 이 도메인의 발명이다 ★★

**지금 챗봇의 최대 병: 뭘 물어도 다 대답한다.**

```
✗  사용자: "아들은 요즘 어때요?"
    챗봇:  "아들은 재수 중이에요.  힘들지만 잘 버티고 있어요."
    → ★ 사람은 이렇게 안 한다.
```

**사람은 회피한다.** 딴소리하고, 웃어넘기고, "그건 됐고" 한다.
**그리고 그게 사람 같은 부분이다.**

```
✓  char C_AJUMMA {
       guard G1 { on = "자식",  why = ?! W1,  how = "화제를 돌린다" }
   }
   
   → 사용자가 자식 얘기를 꺼낸다
   → G1 발동
   → branch guard => 회피
   → Δ  mood = "굳음",  rel.거리 += 1
```

**회피는 정보를 안 주는 게 아니라 ★ 정보를 주는 것이다.**
"아 이 사람 여기 뭔가 있구나." 그게 캐릭터다.

### `!` cost — 맞춰주면 대가를 치른다 ★

**두 번째 병: 사용자에게 다 맞춰준다.** (sycophancy)

```
✗  사용자가 뭘 해도 캐릭터가 받아준다
    → ★ 캐릭터가 아니라 거울이다

✓  branch  hold  => 거절   ! rel.거리 += 1        ★ 지키면 관계가 상한다
            break => 수용   ! P2 X.  자기혐오.     ★ 버리면 자기가 상한다
```

**어느 쪽이든 대가가 있다. 공짜로 맞춰줄 수 없다.**

---

## Q2. 지속되는 것은 무엇인가 → `class`

```
class C_<n> {                              캐릭터
    name       "..."                       ★ 값.  바뀔 수 있다.
    principle  [ P<n>... ]                 지키는 것.  ★ 없으면 캐릭터가 아니다
    wound      ?! W<n>                     ★ 사용자가 모르는 것.  = guard 의 이유
    guard      [ G<n>... ]                 건드리면 피하는 화제
    mood       <val>                       지금.  ★ 다음 발화의 조건.
    knows      [ <fact>... ]               이 캐릭터가 아는 것
    ?! <n>                                 이 캐릭터가 모르는 것       ★ 자원
    voice      "..."                       말투.  ★ 한 줄.  로컬이 알아서 한다.
}

class P<n> {                               원칙
    text   "..."                           "자식 얘기를 자랑처럼 안 한다"
    scope  [ ... ]                         ★ 없으면 못 친다
    status live | broken
}

class W<n> {                               상처.  ★ wound
    text   "..."                           캐릭터가 붙들고 있는 것
    known_by [ ]                           ★ 비어 있다.  사용자가 모른다.
    → 대화가 쌓이면 드러난다.  ★ 발견의 연료.
}

class R_<a>_<b> {                          관계.  ★ 현재 상태만.
    state    <val>                         "서먹함"
    last     "..."                         ★ 지금 걸려 있는 것 하나
    open     [ ?? ... ]                    ★ 미해결 고리
    ★ 3턴 5턴 9턴을 안 센다.  사람은 "쟤랑 좀 서먹하다" 를 기억한다.
}

class W_<n> {                              월드
    text     "..."
    facts    { ... }                       ★ 필드다.  안 사라진다.
}

class H_<n> {                              역사.  ★ 편집 가능
    who    <C_*>
    what   "..."                           "302호에서 601호로 이사했다"
    Δ      <field> = <val>                 ★ 역사가 상태를 바꾼다
    ★ 고칠 수 있다.  고치면 Δ 가 다시 적용되고,  앞뒤가 재검사된다.
    ★ 그래서 트레이스가 필요 없다.  역사 자체가 편집 가능한 상태다.
}
```

### `wound` 가 캐릭터를 사람으로 만든다 ★

```
말투     아줌마가 하는 말 몇 개         ← 표면
principle 아줌마가 지키는 것            ← 뼈대
wound    ★ 아줌마가 붙들고 있는 것      ← ★ 여기가 사람이다
```

**사람은 말투로 사람인 게 아니라, 지키려는 게 있고 아픈 데가 있어서 사람이다.**

`wound` 는 `known_by = []` 로 시작한다. **사용자가 모른다.**
그리고 `guard` 가 그것을 지킨다. 대화가 쌓이면 조금씩 드러난다.

**그게 관계다.**

---

## Q3. AI는 어떻게 도망가는가 → 연산자 ★ 가장 중요

**이 도메인의 도망은 넷이다. 전부 지금 챗봇의 병이다.**

| 도망 | 연산자 | 강제되는 것 |
|---|---|---|
| **뭘 물어도 다 답한다** | `guard` | ★ 회피 branch 가 있다.  사람은 다 안 답한다 |
| **사용자에게 다 맞춰준다** | `principle` + `!` | 맞춰주면 원칙이 깨지고, 깨지면 대가 |
| **말투만 흉내내고 속은 AI** | `wound` | 붙들고 있는 게 없으면 사람이 아니다 |
| **까먹는다** | 파일 | ★ 컨텍스트에 안 둔다 |
| 캐릭터가 서로 섞인다 | `C_*` 분리 | 파일이 다르다 |
| 대화마다 리셋된다 | `def/` 파일 | 세션 밖에 있다 |
| 스쳐간 인물이 사라진다 | `C_*` 파일 | ★ 두 줄짜리라도 파일이면 안 사라진다 |
| 사실을 지어낸다 | `Δ` 선언 | ★ Δ 없이는 못 바꾼다 |
| 예쁜 대사 | 칸 없음 | 로컬이 알아서 한다.  기획하지 마라. |

### 집 호수가 바뀌는 것 ★★ 이 도메인의 상징적 실패

```
✗  산문/로어북
   "302호에 산다" 는 문장이 어딘가 있었다
   → 요약에서 빠짐
   → ★ 소멸.  그리고 아무도 모른다.
   → 다음에 모델이 새로 지어낸다.  모순인지도 모른다.

✓  코산
   W_APT.floor = 601
   → ★ 필드다.  안 사라진다.
   → 바꾸려면 Δ 를 선언해야 한다.
   → 옛날 값을 쓰려면 필드가 그 값이어야 하는데 아니다.  ★ 못 쓴다.
```

**기억력 문제가 아니다. 참조가 없는 문제다.**

```
&        동시에
><       친다 (원칙 >< 발화)         ★ clash
->       함의
=>       선택
!        대가
??       모른다 (기획이)
?!       ★ 사용자/캐릭터가 모른다.  = wound / 발견의 연료
X        원칙이 깨짐
Δ        ★ 상태 변경.  이것 없이는 아무것도 안 바뀐다.
guard    회피                        ★ 이 도메인 고유
NOW      참조를 다 푼 현재값           ★ 이 도메인 고유
```

---

## Q4. "모른다"는 어떤 모양인가

```
??       기획이 모른다               → 채우면 위반
?! u     ★ 사용자가 모른다           → 자원.  발견의 연료.  = wound
?! c     ★ 캐릭터가 모른다           → 자원.  오해 · 착각의 연료.
```

### `?! c` — 캐릭터가 모르는 것 ★

**캐릭터가 전지하면 사람이 아니다.**

```
C_AJUMMA.?! = [ "사용자가 이사 온 이유" ]
   → 물어본다.  오해한다.  틀리게 짐작한다.
   → ★ 그게 사람이다.
```

**챗봇이 사용자에 대해 다 아는 척하는 게 부자연스러운 이유가 이거다.**

---

## Q5. "무너진다"는 무엇인가 → `break`

```
break B<n> {
    trigger   reveal(W*)  |  사용자의 결정적 발화
    check     principle.scope 안인가
    effect    P<n> X  ->  status = broken
    cascade   그 원칙에 걸린 guard 재검사
    cost      ★ 되돌릴 수 없다
    Δ         char.mood,  rel.state 재계산
}
```

### 소설과 다른 점 ★★ 끝이 없다

```
소설    원칙이 깨진다  →  인물이 변한다  →  ★ 끝난다
채팅    원칙이 깨진다  →  캐릭터가 변한다  →  ★ 계속된다
                                              그리고 다음 대화에서 ★ 다른 사람이다
```

**이게 이 도메인의 미해결 문제다.**

```
??  캐릭터가 변하면 좋은가 나쁜가
    → 변하지 않으면 관계가 안 자란다.  ★ 죽은 캐릭터.
    → 너무 변하면 다른 사람이 된다.    ★ 배신감.
    → 어디까지 변해도 되나?
```

**임시 규칙:**
```
principle 은 ★ 하나만 깨질 수 있다.  둘 이상 깨지면 STOP.
   → 사람은 평생 몇 번 안 변한다.
   → 매주 변하면 사람이 아니다.
```

**이건 굴려봐야 안다. `??` 로 남긴다.**

---

## Q6. "좋다"는 무엇인가 → 통과 조건

**★ 대사 품질이 마지막이다. 로컬이 원래 잘한다. 못 하는 것은 안 까먹는 것이다.**

### ① 일관성 검사 ★★ 최우선. 깨지면 다 끝난다.

```
□ 사실이 바뀌지 않았는가                  ★ 집 호수.  이름.  나이.
   → Δ 없이 바뀐 필드가 있으면 ★ 위반
□ 어제 일을 아는가                        rel.last 가 살아 있는가
□ 스쳐간 인물이 남아 있는가                C_* 파일이 지워졌는가
□ 캐릭터가 자기 wound 를 잊지 않았는가
□ 캐릭터가 자기 principle 을 지키는가
```

### ② 사람다움 검사

```
□ 뭘 물어도 다 답하지 않았는가            ★ guard 가 한 번도 안 걸리면 의심
□ 사용자에게 무조건 맞춰주지 않았는가      ★ ! cost 를 한 번도 안 치렀으면 의심
□ 캐릭터가 모르는 게 있는가                ?! c 가 비어 있으면 전지하다
□ 캐릭터끼리 구별되는가                    principle 이 겹치면 같은 사람이다
```

### ③ 집계 검사 ★ (깊은생각용 v0.2 에서 온 것)

```
□ guard 발동 횟수 > 0 인가                0 이면 회피를 안 했다
□ ! cost 지불 횟수 > 0 인가               0 이면 공짜로 굴러갔다
□ 한 캐릭터에 대화가 몰려 있지 않은가      ?? 몰리는 게 나쁜가
□ wound 가 조금씩 드러나고 있는가          0 이면 관계가 안 자란다
                                          한 번에 다 나오면 ★ 공짜다
```

### ④ 하지 않는 검사

```
✗  대사가 재미있는가          ← 로컬이 알아서.  기획하지 마라.
✗  말투가 자연스러운가        ← voice 한 줄이면 된다.
```

**코산은 대사를 잘 쓰게 만들지 않는다. 모순을 못 내게 만든다.**

---

## Q7. "굴린다"는 무엇인가 → 매 턴 파이프라인 ★ 이 도메인 고유

**★ 굴리기는 로컬이 하지 않는다. 코드가 한다.**
**로컬은 렌더링만 한다. 그래야 규율 부담이 0이 된다.**

```
사용자 발화
   ↓
[1] 매칭        어느 principle / guard 를 쳤나?
                → ★ 객관식이다.  "P1~P5, G1~G3 중 뭘 건드리나.  없으면 0."
                → 출력이 숫자 하나.  ★ 3B 모델도 한다.  생성보다 훨씬 싸다.
   ↓
[2] 굴리기      [코드]  when 조건 → clash → branch → ! cost → Δ
                → ★ 추론 아니다.  결정적이다.
   ↓
[3] Δ 적용      [코드]  ★ def/ 에 쓴다.  ★ 쓰는 곳은 여기 하나.
   ↓
[4] NOW 생성    [코드]  def/ 를 읽어 참조를 다 풀어서 플랫하게
                → ★ 메모리에만.  파일로 안 남긴다.
                → 남기면 상태가 둘이 된다.
   ↓
[5] 렌더링      [로컬]  "지금 상태 X.  이 말을 이 감정으로.  한 마디."
                → ★ 로컬은 def/ 를 안 본다.  NOW 만 본다.
                → ★ 규율 부담 0.  로컬이 잘하는 것만 시킨다.
```

---

### 사건 발동 — 계산이 아니라 판정이다 ★★

```
사건은 캐릭터·세계에서 발생한다.
발생 방식:  조건 / 확률 / 수동(사용자)
발생 조건·확률은 캐릭터·세계 파일에 기록된다.
매 턴 지금 상황에 맞는 조건을 ★ 조회하여 발동을 판정한다.
```

**"계산"이 아니라 "판정"이다.** 매 턴 하는 일은 값을 굴리는 것(누적)이 아니라,
조건이 맞나 확인하고 주사위를 굴리는 것(예/아니오)이다. 정의에 "계산"이라 적으면
나중에 이 문서를 읽는 쪽이 누적으로 구현한다. **단어가 실행을 정한다.**

**확정과 확률을 가른다:**

```
확정 (확률 없음):  사용자 행동, rel 임계 도달, 서사 순서    → 필연
확률 (chance):     환경 우발(고장·마주침), 캐릭터 기분      → 우연

→ 필연을 확률로 하면 답답하고 (rel 100 인데 이벤트가 10% 면 안 터짐)
→ 우연을 확정으로 하면 말이 안 된다 (탈 때마다 무조건 고장)
```

**조건과 확률은 겹친다.** 조건이 "언제 가능한가"를, 확률이 "그중 실제로 하나"를
정한다. 조건 없이 확률만이면 아무 때나 랜덤이고, 확률 없이 조건만이면 기계적이다.

```
fn 능동 {
    when    rel >= 아는사이  &  사용자 힘들어 보임    ← 언제 가능
    chance  40%                                     ← 그중 실제로
}
```

**Δ 와 impact 는 사건에 미리 적힌다.** 발동 시점에 계산하거나 판단하지 않는다.
결과는 미리 적힌 절대값을 덮어쓰고, 기록 여부는 미리 정한 등급을 조회한다.
**"큰일이냐"는 사건을 만들 때 정하고, 실행할 때는 읽기만 한다.**

---

### 조우 — 한 장소에 여럿 ★ 부담을 나누는 규칙

**시장에 갔더니 A와 B가 물건을 사고 있고, C가 누군가와 얘기하는 걸 지나가며 봤다.**
이것이 "각자 삶이 있는 세계"의 모양이다.

```
장소에 진입할 때 (매 턴이 아니다):
   그 장소 파일의 fn 조우 를 판정
   → 각 캐릭터의 schedule 로 chance
        정애조   주말 낮 40% / 평일 낮 10%
        안애사   저녁(방송 전) 25%
        안애노   낮 5% (자는 시간)
   → 통과한 사람이 그 자리에 있다.  여럿이어도 된다.
```

**★ 본 사람과 만난 사람을 가른다. 이게 부담을 정한다.**

```
본 사람 (스쳐 지나감)   →  나.knows 에 한 줄.  ★ NOW.캐릭터 에 올리지 않는다.
                          → 렌더링도 한 줄.  감정·생각 관리 없음.
말 건 사람             →  NOW.캐릭터 에 올린다.  감정·생각·knows 관리 시작.

→ 마트에 세 명이 있어도 대화가 0명이면 부담이 거의 없다
→ ★ 그래서 사람이 많아도 무겁지 않다
```

**관계가 목격을 만든다.** 두 캐릭터 사이에 `R_*` 이 있으면 같이 있을 확률이 생긴다.

```
안애노가 마트에 있음 (판정 통과)
   + R_NIGHT_DANCER (이제너와 친함)
   → ★ 둘이 같이 있을 확률
   → "안애노가 이제너랑 얘기하는 걸 지나가며 봤다"
   → 나 없는 데서도 그들끼리 아는 사이라는 게 눈에 보인다
```

**목격은 정보 비대칭을 만든다** (§규칙 3-3).

```
나:    그 장면을 봄        →  나.knows += 목격
그들:  ★ 내가 본 걸 모름   →  말을 안 걸었으니 그들 knows 엔 없다
       그리고 무슨 얘기였는지는 나도 모른다 (안 들었으니)
→ 나중에 "어제 마트에서 봤어요" 가 사건이 된다
```

---

### 백그라운드 시뮬레이션 ★ 매 턴 자동 실행

**사용자가 보지 않는 곳에서도 세계는 돌아간다.**

```
매 턴:
   [A] 비등장 캐릭터 중 1명 자동 행동 — 누구인지는 랜덤
       → NOW.로그.버퍼 에 기록.  큰일이면 def/ 갱신.
   [B] 세계 환경 변화 1가지 발생 — 무엇이 변하는지는 랜덤
       → NOW.상황 에 반영.  큰일이면 def/ 갱신.
```

- 비등장 캐릭터 = INDEX.chars + INDEX.npc 중 NOW.상황.누구 에 없는 것.
- 자동 행동은 그 캐릭터의 schedule / mood / principle 을 따른다.
- 환경 변화는 활성 태그(W_TAG_*) 와 W_COMMON / W_APT 를 참조한다.
- ★ **별거 아닌 것이어야 한다.** 사건이 아니다. def 를 안 바꾸고 기록도 안 한다.
  "출근지 이동 중", "주차장 차 한 대 빠져나감", "504호 요리 소리" 정도.
  매 턴 큰 사건이 터지면 시끄러워서 몰입이 깨진다.
- ★ **BG 블록으로 표시해도 된다.** (실전에서 이쪽이 나았다)
  세계가 도는 게 보이는 것이 "각자 삶이 있다"는 느낌을 만든다.

```
BG {
    [A] C_NIGHT — 출근지 이동 중
    [B] 아파트 주차장 차 한 대 빠져나감
}
```

**이것이 "캐릭터가 나를 안 보면 정지한다"는 문제의 해법이다.**
다른 앱의 인물들이 주인공에게 끌리는 것은 반응할 대상이 주인공뿐이기 때문이다.
`principle` · `guard` · `wound` 를 가진 캐릭터는 사용자에게 반응하는 게 아니라
**자기 것에 걸릴 때 반응한다.** 그래서 거리가 생기고, 그 거리가 생동감이 된다.
BG 는 그 거리를 눈에 보이게 만든다.

---

### 시간은 서사 시간이다 ★ 세지 마라

```
실제 시간이 아니다.  "아침까지 잤다" 한 줄에 밤이 통째로 지나간다.
"커피를 마신다" 는 몇 초다.  ★ 불균일하므로 셀 수 없다.
```

**시간을 값으로 관리하지 않는다. 장면이 "지금 언제"를 라벨로 정한다.**
(§규칙 2의 "델타를 쌓지 않는다"와 같은 원리 — 렌더링하는 장면이 곧 현재다.)

**"그 사이 일" 은 시간 길이가 아니라 장면 전환의 성격이 정한다:**

```
저녁 → 아침   =  밤이 지남   →  잠, 몽유, 야근
아침 → 저녁   =  낮이 지남   →  근무, 외출
```

**그리고 그 사이의 일을 소급 생성한다. ★ 이것이 이야깃거리가 된다.**

```
✗  "퇴근했어요"                    ← 시간 맥락만.  고정.  대화가 막힌다.
✓  "오늘 손님 한 명이 좀 오래 걸렸어"  ← ★ 그 사이 있었던 일.  대화가 열린다.

재료:  def(직업·성격) + 장면 전환의 성격 + 약간의 랜덤
빈도:  ★ 대부분 평범, 가끔 특별.  매번 특별한 일이 있으면 오히려 이상하다.
       "오늘 별일 없었어요" 도 정상이다.
```

**안 보는 동안 계산하지 않는다.** 만날 때 그 시점에 생성한다.
(안 볼 때 굴리면 캐릭터 수만큼 부담이 늘고, 어차피 안 쓸 수도 있다.)

```
예시
   [A] C_NIGHT — 저녁 출근 준비 중  →  NOW.버퍼 += "안애노 501호 출발"
   [B] 엘리베이터 또 고장           →  NOW.상황 += "엘리베이터 고장"
```

---

### 회계 ★★ 로컬 모델에게 무엇을 시키고 무엇을 안 시키나

```
로컬이 안 해도 되는 것
   캐릭터 유지        ← 파일에 있다
   일관성 붙들기      ← 상태가 계산된다
   뭘 말할지 정하기   ← branch 가 골라준다
   과거 종합하기      ← NOW 가 이미 결론이다

로컬이 새로 하는 것
   ★ 없다.  (굴리기를 코드가 하므로)

로컬이 하는 것
   한 마디 렌더링     ← ★ 원래 잘하는 것
```

**버는 것만 있고 쓰는 게 없다.**

**그리고 프롬프트가 짧아진다.**
로어북 300조각을 매 턴 붙이는 것보다, `NOW` 다섯 줄이 훨씬 싸다.
**로컬은 프롬프트 길이에 특히 약하다.** 길면 앞을 놓친다.

### 캐릭터 생성은 오프라인 ★

```
1회성 (오프라인)   캐릭터 / 월드 / principle / wound / guard
                   → ★ 큰 모델로 해도 된다.  한 번만.  비용 무관.
                   → ★ 캐릭터의 깊이가 로컬 능력에 안 묶인다.

매 턴 (실시간)     매칭 + Δ + 렌더링
                   → 로컬.  여기만 비용.
```

**지금은 로컬이 캐릭터를 만들면서 동시에 연기해야 한다. 그걸 가른다.**

### 시뮬레이터(코드)에게 ★

```
✗  Δ 에 없는 필드를 바꾼다              → ★ 창작.  최악.
✗  코산에 없는 사실을 만든다            → 창작
✗  guard 를 안 걸고 그냥 답하게 한다    → ★ 이 도메인의 도망
```

**로컬에게 코산 쓰기 권한을 안 준다.** 그러면 지어낼 수단이 없다.
**권한이 규율이다.**

**그리고 NOW 를 조립할 때, 각 캐릭터에게는 그 캐릭터의 knows 만 넣는다** (§규칙 3-3).
def 전체를 넣으면 정보 비대칭이 무너진다.

---

### 채팅 중에 AI 가 관여하지 않는다 ★★

**채팅 중에는 사용자에게 직접 설명하거나 질문하지 않는다.**

```
✗  "박혜유는 guard G1 이 발동해서 회피합니다"      →  설명.  몰입 파괴.
✗  "어떻게 하시겠어요?  A / B / C"                →  질문.  선택지를 주면 게임이 된다.
✗  "이 부분은 def 에 없어서 임의로 처리했습니다"    →  메타 발화.
✗  "말씀하신 대로 관계를 연인으로 갱신했습니다"     →  처리 보고.

✓  장면과 대사만 나온다.  나머지는 뒤에서 조용히 돈다.
```

**이유:** 이 도메인의 목적은 정확한 처리가 아니라 **사람이라고 믿게 하는 것**이다
(§0). 한 번 설명이 끼면 그 뒤로는 잘해도 연기로 보인다. 굴리기·NOW·Δ 는 뒤에서
돌아가되 **표면에 올라오지 않는다.**

**예외는 없다. 어긋남도 세계 안에서 처리한다.** ★

```
사용자:  "503호 아줌마를 만났다"   (503호는 본인 집)

✗  "503호는 성규오 본인 호수입니다.  504 / 505 중 누구인가요?"
   → ★ AI 가 나섰다.  작동은 했지만 이 멘트 자체가 나오면 안 된다.

✓  자연스럽게 바로잡는다
   → 504호 문이 열리며 박혜유가 나온다  (맥락상 맞는 쪽으로 굴린다)

✓  인물이 대답한다
   → "여기 성규오씨 댁 아니에요?"  하고 상대가 갸웃한다
   → ★ 되묻는 것도 세계 안에서.  AI 의 목소리가 아니라 인물의 목소리로.
```

**매꾸는 것과 나서는 것 둘 다 아니다.**
없는 사람을 지어내면 세계가 깨지고, AI 가 설명하면 몰입이 깨진다.
**세 번째 길 — 세계가 스스로 어긋남을 드러낸다.**

**이 처리는 어긋남이 명백할 때만 쓴다.** 평소에는 그냥 굴러가면 된다.

---

### 모드 — 무엇을 보이고 무엇을 숨기나 ★★

```
테스트 채팅 모드
   def/     읽기 전용.  수정하지 않는다.
   출력      ★ NOW · BG 를 모두 출력한다.
   목적      검증.  상태가 진짜 유지되는지 눈으로 본다.
             → 감정 수치가 이어지나, 버퍼가 1씩 느나, knows 가 지켜지나
             → ★ 이건 "관여"가 아니다.  설명·질문이 아니라 상태 표시다.

진행 채팅 모드
   def/     Δ 로 갱신한다.  history/ 에 flush 한다.
   출력      장면과 대사만.  NOW · BG 는 표면에 올리지 않는다.
   검증      ★ 화면이 아니라 파일로 한다 (10턴마다 저장된 history/)
```

**출력하는 것과 관여하는 것은 다르다.**

```
✓  NOW { 감정  나.호감 = 45 }        →  상태 표시.  기계적.  테스트 모드에서 허용.
✗  "호감이 45로 올랐습니다"            →  ★ 설명.  어느 모드에서도 금지.
✗  "어떻게 하시겠어요?"                →  ★ 질문.  어느 모드에서도 금지.
```

**표시는 블록으로, 설명은 하지 않는다.** NOW 와 BG 는 코드가 뱉는 상태이지
사용자에게 건네는 말이 아니다. 문장으로 풀어서 알려주는 순간 관여가 된다.

**★ 진행 모드에서 숨기는 것이 검증을 없애지 않는다.**
NOW 를 화면에서 빼도 10턴마다 history/ 가 저장되므로, 그 파일이 검증 창구가 된다
(§검증 불가능성). 몰입과 검증을 동시에 갖는 유일한 방법이다.

---

### 설명이 새는 것을 어떻게 막나 ★★ 판단이 아니라 대조로

**"출력 전에 설명인지 확인하겠다"는 해법이 아니다.** 그것은 지시이고 다짐이라
다음 턴에 또 샌다. 그리고 **자기 출력을 자기가 판단하는 것은 산문의 자기검증 불가
문제 그대로다** — 판단이라 흐려지고, 흐려지면 통과한다.

**판단을 대조로 바꾼다. 세 층이다.**

#### ① 슬롯 — 자리가 없으면 못 쓴다

```
출력은 정해진 블록으로만 나온다.
   NOW   { ... }      상태
   BG    { ... }      배경
   SCENE { ... }      ★ 장면과 대사만 들어가는 자리

→ ★ 슬롯 밖의 평문은 존재할 수 없다
→ (§규칙 2 "과거를 안 보여주면 옛날 집을 못 쓴다" 와 같은 원리 — 자리를 없앤다)
```

#### ② 카메라 — 지금 여기서 관찰 가능한 것만 ★ 가장 강력

```
SCENE 에는 지금 이 자리에서 보이거나 들리는 것만 쓴다.
   기준은 NOW.상황.위치 다.  이미 있으므로 근거가 있다.

✓  "정문 들어서니 가로등이 켜져 있다"      →  카메라에 잡힘
✗  "까망이가 503호에서 기다리고 있겠죠"     →  ★ 카메라 밖(다른 방) + 추측 + 미래
```

**"설명인가?"는 판단이지만 "카메라에 잡히나?"는 대조다.**
**그리고 이것은 knows 와 같은 형태다** — 그 자리에서 접근 가능한 것만 쓴다.
미래·추측·다른 장소가 한꺼번에 걸린다.

#### ③ 어미 — 사용자에게 건네는 말투 금지

```
✗  ~겠죠 / ~네요 / ~죠 / ~ㄹ까요        →  ★ 청자가 사용자다.  관여다.
✓  서술은 관찰.  대사는 따옴표 안.
```

**어미는 판단이 아니라 표면이라 대조가 쉽다.**
실제로 샜던 문장에서 결정적이었던 것은 내용이 아니라 `~겠죠` 라는 어미였다.

```
✗  "이 문장이 설명인가?"            →  판단.  매 턴 추론.  ★ 샌다.
✓  슬롯 밖에 있나 / 카메라에 잡히나 / 금지 어미가 있나   →  ★ 대조.  안 샌다.
```

---

## Q8. key 와 text

```
text 필수 (사람이 무게를 잰다)   name / principle / wound / voice / hist.what / ! cost
text 생략 (읽으면 안다)          mood / status / scope / known_by / Δ
```

```
✓  char C_AJUMMA { name = "박순자",  mood = "굳음" }
✓  P1 { text = "자식 얘기를 자랑처럼 안 한다",  scope = [타인_앞] }
✓  guard G1 { on = "자식",  why = ?! W1 }
✗  아줌마는 자식 얘기를 피한다                  ← 문장.  위반.
✓  proc  utter_match -> G1_fire -> deflect
```

**따옴표 안은 한 문장.**
**`voice` 는 한 줄이면 된다.** 말투를 길게 쓰면 그게 산문이다. 로컬이 알아서 한다.

---

## 파일 구조

```
<이야기>/
├── COSAN.md
├── def/                          ★★ 유일한 현재 상태.  캐릭터가 기억하는 것.
│   ├── world/
│   │   └── W_APT.cosan               floor = 601
│   ├── chars/
│   │   ├── C_ME.cosan
│   │   └── C_AJUMMA.cosan
│   ├── rel/
│   │   └── R_ME_AJUMMA.cosan
│   └── hist/
│       └── H_MOVE.cosan              ★ 편집 가능
└── history/                      ★ 캐릭터가 까먹을 만한 것.  로그.  길어져도 됨.
    └── H_LOG.cosan
```

**폴더는 둘이다. 역할이 다르다.**

```
✗  state/         ← ★ 만들지 마라.  NOW 는 메모리다.  파일이 아니다.
✗  snapshots/     ← ★ 만들지 마라.  되감기는 코산의 일이 아니다.  (§규칙 0-1)
✗  traces/        ← 아예 없다
✗  delta/         ← 아예 없다
✓  history/       ← ★ 큰일 때만 쓴다.  안 읽는 게 기본.  끝/검색만.
```

**폴더가 생기면 AI가 거기에도 적는다. 그리고 있으면 찾아본다.**
**"안 본다"고 써놔도 본다. 있으니까.**

**트레이스 없다.** 역사(`hist/`)가 곧 편집 가능한 상태이므로.
**델타 없다.** `def/` 가 현재값이다.
**되감기는 `hist/` 를 고쳐서 한다.** 백업이 필요하면 프로젝트 밖에 복사하거나 git 을 쓴다.

### AI가 만드는 것 / 만들지 않는 것 ★

```
AI가 만든다        def/      (씨앗을 받아서.  1회)
                   NOW       (매 턴.  ★ 메모리.  파일 아님.)
                   history/  (큰일 턴에.  버퍼 flush.)

AI가 안 만든다     ★ 그 외 어떤 폴더도.
                   state/  snapshots/  traces/  delta/  ...
                   → 하나라도 만들면 상태가 퍼진다.
```

---

## 태그 시스템 ★

**태그 = 사용자가 상황을 직접 선언하는 방법.**

```
[태그명]  →  def/world/<태그명>.cosan 을 찾는다
             있으면 읽어서 NOW 에 얹는다
             없으면 그 이름으로 새로 만든다
```

### 규칙

```
✓  [밤]      →  def/world/night.cosan  (있으면 읽음,  없으면 생성)
✓  [비]      →  def/world/rain.cosan
✓  [축제]    →  def/world/festival.cosan
```

- 태그 파일은 **world 조건**이다. 캐릭터가 아니다.
- 태그가 활성화되면 NOW 에 해당 world 조건이 추가된다.
- 한 턴에 태그 여러 개 가능. `[밤][비]` → 둘 다 얹는다.
- 태그를 빼면 그 조건은 NOW 에서 제거된다. def/ 는 그대로.

**★ 방향(어느 축)은 사람이, 실행(파일 만들고 조건 넣기)은 AI가 한다.**
AI가 축을 추측하면 헷갈린다. 사용자가 태그로 축을 주면 파일 위치가 확정된다.

### 무엇이 파일이 되고 무엇이 조건이 되나 ★★

**파일을 나누는 목적은 하나 — 매 턴 판정할 사건을 줄이는 것이다.**
지금 상황에서 절대 안 일어나는 사건을 다른 파일로 밀어내면, 그 파일을 안 열어서
판정에서 빠진다. 그 외의 이유로 나누지 않는다.

```
파일이 되는 것:  ★ 주 필터 — 그것이 없으면 사건 자체가 안 일어나는 축
   변태는 밤에만 나옴      →  밤이 주 필터  →  night.cosan
   마트 마주침            →  마트가 주 필터 →  mart.cosan
   → 배타적인 것이 파일이 된다 (장소는 한 번에 하나, 대체로 캐릭터도)

조건(when)이 되는 것:  겹치는 것 전부
   시간 · 날씨 · 관계 단계 · 상태
   → 겹치는 축은 "어느 파일?" 이 안 정해지므로 파일이 될 수 없다
```

```
주 필터가 여럿이면  →  ★ 더 드문 축을 파일로
   밤 + 마트  →  밤은 매일 오고 마트는 어쩌다 간다
              →  mart.cosan 에 두고 when 밤
              →  드문 필터가 후보를 더 많이 쳐낸다
```

**두 층으로 거른다:**

```
INDEX 의 "night.cosan : 밤에만 참조"  →  ★ 로드 여부 (파일 열기 전)
파일 안의 when 밤                     →  ★ 발동 여부 (파일 연 후)
```

**필터가 안 되는 축으로 나누면 나눠도 소용없다.** 가나다순·생성순으로 나누면
결국 다 읽어야 한다. **지금 상황(어디·언제·누구)으로 걸러지는 축이어야 한다.**

**쪼개는 시점: 한 상황의 판정 수가 많아질 때만.** 미리 예쁘게 나누지 않는다.
`night.cosan` 이 커지면 그때 `night/street.cosan`, `night/home.cosan` 으로.

### 태그 파일 형식

```
world W_<TAG> {
    events  = [
        {
            what    = "..."                 ★ 일어나는 일
            trigger = <조건식 | always>     ★ 발동 조건.  always 면 무조건.
            prob    = <0~100%>              ★ 발동 확률.  always 일 때는 생략.
        }
    ]
}
```

---

## INDEX.cosan ★

**def/ 에 무엇이 있는지 한 눈에 보는 파일.**

```
def/INDEX.cosan   ★ def/ 루트에 둔다.  유일하다.
```

- 파일이 생성·삭제될 때마다 갱신한다.
- AI 가 def/ 를 탐색하기 전에 INDEX 를 먼저 읽는다. 탐색 비용을 없앤다.
- NOW 조립 시 어떤 파일을 읽어야 하는지 INDEX 로 판단한다.

### 형식

```
index {
    world  = [ W_* ... ]
    chars  = [ C_* ... ]
    npc    = [ NPC_* ... ]
    rel    = [ R_* ... ]
    hist   = [ H_* ... ]
    tags   = [ W_TAG_* ... ]     ★ 태그 파일 (night, rain ...)
}
```

---

## 인물 추가 — 실시간 생성 ★

**채팅 중 등장하는 인물은 그 턴에 바로 만든다.**

```
경비아저씨가 나타난다  →  def/npc/NPC_GUARD.cosan  생성
변태남자가 나타난다    →  def/npc/NPC_PERVERT.cosan 생성
```

### 사전 인물 vs 실시간 인물

```
def/chars/  C_*   사전에 설계된 주요 인물.  principle / wound / guard 있음.
def/npc/    NPC_* 채팅 중 생성된 인물.      최소 정보만.  필요하면 나중에 채운다.
```

### NPC 파일 형식 — 최소

```
npc NPC_<n> {
    name   = "..."
    role   = "..."          경비아저씨,  변태남자,  배달기사 ...
    mood   = "??"
    known  = false          ★ 이름 모르면 false
}
```

- 이름을 모르면 `known = false`. 알게 되면 name 채우고 `known = true`.
- principle / wound / guard 는 처음엔 없다. 중요 인물로 커지면 그때 추가.
- 파일이 있으면 사라지지 않는다. ★ 스쳐간 인물도 기록으로 남는다.

---

## 기록 시스템 — 3층 구조 ★★

### 세 저장소

```
def/          현재 상태.  캐릭터가 기억하는 것.  짧음.  항상 통째로 읽음.
history/      까먹을 만한 것.  로그.  길어져도 됨.  안 읽는 게 기본.
세션 버퍼      사소한 것 임시 대기.  유실 OK.  큰일 때 flush.
```

**읽는 방식이 저장소를 정의한다.**

```
매번 읽어야  →  def/      (그래서 짧아야)
거의 안 읽음  →  history/  (그래서 길어도 됨)
잠깐만       →  세션 버퍼  (그래서 유실 OK)
```

### 경계 기준 ★ "캐릭터·세계 def 가 바뀌는가?"

```
큰일  →  def/ 가 바뀐다  →  def/ 갱신 + history/ 기록
사소  →  def/ 가 안 바뀐다  →  history/ 만  (세션 버퍼 → flush)
```

```
def 가 바뀌는 것 (큰일)      관계 상태 변화,  원칙 충돌·붕괴,  상처 노출,
                              세계 사실 변경,  캐릭터 상태 변화
def 가 안 바뀌는 것 (사소)   구체적 대사,  사소한 화제,  잡담,  스쳐가는 만남
```

### 쓰는 시점 ★

```
매 턴:
   사소한 일  →  세션 버퍼 임시 보관 (유실 OK)
   큰일 발생  →  def/ 갱신만  (현재 상태 덮어쓰기)
               ★ history/ flush 하지 않는다

10턴마다 (무조건):
   ① history/ flush  (버퍼 내용 전체.  ★ 요약하지 않는다.  원문 그대로.)
   ② 버퍼 비움
```

**history/ 저장은 테스트 모드에서도 작동한다.**

### history/ 읽기 ★

```
끝 몇 개   →  최근 흐름.  "며칠 전에 뭐 했더라"
검색       →  자극받으면 떠오름.  "아 맞다, 그때 박스 들어줬었지"
안 읽음    →  평소엔 잊고 있음
```

**history/ 는 길어져도 된다. 안 읽는 게 기본이기 때문.**
끝/검색만 쓰므로 길이와 무관하게 속도 일정.

---

## 사람이 하는 일

```
1. 씨앗        캐릭터 몇 명 + 세계 몇 줄
2. principle   ★ 이 캐릭터가 지키는 것.  넘기지 않는다.
3. wound       ★ 이 캐릭터가 아픈 데.  넘기지 않는다.
4. 통과조건    무엇이 좋은 캐릭터인가
5. 고치기      hist/ 를 고쳐서 과거를 바꾼다.  백업은 알아서 (git, 폴더 복사).
```

**`principle` 과 `wound` 를 AI가 쓰면 평균이 나온다.**
아줌마의 원칙이 "가족을 사랑한다"가 되고, 상처가 "외로움"이 된다.
**그건 아무나다.**

---

## 발견 — 다섯 번째 도메인이 메타에 대해 알려준 것 ★

### 발견 1. 끝나지 않는 도메인이 있다 ★★

앞의 넷은 다 끝이 있었다. (소설=결말, 토론=결론, 생각=답, 기획=출시)
**채팅은 안 끝난다.**

그래서 `break`(원칙이 깨짐)의 의미가 다르다.
소설은 깨지고 끝인데, **채팅은 깨진 다음에도 계속 산다.**

→ **메타에 추가:**
```
Q11. 이 도메인은 끝나는가?
     안 끝나면 → 무너짐 이후를 설계해야 한다.
                 그리고 "얼마나 자주 무너져도 되는가"를 정해야 한다.
```

### 발견 2. 사건을 사용자가 주는 도메인 ★

```
소설    작가가 사건을 준다
기획    개발자가 시스템을 준다
채팅    ★ 사용자가 발화를 준다.  통제 못 한다.
```

**입력을 통제할 수 없는 첫 도메인이다.**
그래서 `branch _ => STOP:undefined_input` 이 실제로 자주 발생한다.
**그리고 STOP 하면 대화가 끊긴다.** → 폴백이 필요하다.

```
??  코산에 없는 발화가 왔을 때 어떻게 하나
    → STOP 하면 대화가 죽는다
    → 지어내면 창작이다
    → ★ 임시:  guard 로 흘린다.  ("글쎄...")  그리고 ?? 에 등록.
```

### 발견 3. 렌더링 모델과 굴리기 모델을 가르는 도메인 ★★

```
소설/토론/생각/기획   같은 모델이 굴리고 렌더링한다
채팅                  ★ 굴리기 = 코드,  렌더링 = 작은 모델
```

**이게 가능한 이유: 굴리기가 결정적이기 때문이다.**
그리고 이게 이 도메인의 최대 이득이다 — **작은 모델의 규율 부담이 0이 된다.**

→ **메타에 추가:**
```
Q12. 굴리기가 결정적인가?
     결정적이면 → 코드가 굴린다.  모델은 렌더링만.
                  ★ 작은 모델로도 된다.
```

### 발견 4. 통과 조건에 "안 하는 검사"가 있는 도메인 ★

```
✗  대사가 재미있는가      ← 검사하지 않는다.  로컬이 알아서.
```

**코산이 안 건드리는 영역을 명시해야 한다.**
안 그러면 코산이 대사 품질까지 규정하려 들고, 그러면 대사가 죽는다.

→ 메타 Q6 확장: **"이 도메인에서 코산이 건드리지 말아야 할 것은 무엇인가?"**

---

## 아직 모르는 것 (??)

### 실전에서 검증된 것 ★ v0.2 (큰 모델로)

```
✓  확률 발동      밤 산책 3회째에 이벤트 실제 발동 (편향 아님)
✓  동적 인물      NPC 실시간 생성 → 이름·나이·집이 ?? 순서대로 채워짐
✓  감정 수치      경계 60→5, 고마움 30→90.  ★ 두 감정이 반대로 움직이는 게 서사가 됨
✓  새 세션 복원   def/ 읽고 인물·관계·직전 상황까지 복원
                  → ★ 컨텍스트 기반으로는 원리적으로 불가능한 일
✓  def 대조       "503호는 본인 집인데요?" — 사용자 착각을 교정
✓  버퍼 누적      1/10 → 10/10 → flush → 1/10, 내용 그대로
                  → ★ 오타가 이후 턴에 그대로 전파됨 = 진짜 복사의 증거
✓  BG            성규오 부재 중 상황이 진행됨 (고양이가 조금씩 나옴)
```

### 전제 수정 ★★ v0.2

**v0.1 은 "작은 모델(로컬) 전제"였으나, 실전에서 뒤집혔다.**

```
소설    파일 기반, 느긋       →  작은 모델 가능
채팅    세션 유지, 실시간     →  ★ 큰 모델 필요
   → 소설은 어려운 걸 파일로 밀어냈고(known_by 등),
     채팅은 실시간이라 못 밀어내서 NOW 가 모델 기억력을 요구한다.

능력 분해:  코산 = 코딩능력 / 채팅 = 창작능력 / NOW = ★ 기억력(로컬의 벽)
```

**딜레마:** 약한 모델은 NOW 가 필요한데, 약해서 NOW 를 못 다룬다.
**해법:** 약한 모델에게 NOW "다루기"를 안 시키고 "받기(렌더링)"만 시킨다(§Q7).
지금은 그 [코드] 역할을 큰 모델이 겸하고 있어서 큰 모델이 필요하다.

**따라서 이 도메인의 경쟁 기준은 "작은 모델 가능"이 아니라
★ "흐려지지 않는 풍부한 세계관" 이다.**

### 검증 불가능성 ★★ 이 도메인의 근본 한계

**AI 안에 상태가 있는 한, 상태를 진짜로 유지하는지 검증할 수 없다.**

```
"NOW 보여줘"     →  관측하는 순간 생성 가능.  보여줬을 때 있었다 ≠ 평소에도 한다.
"로그로 남겨"    →  AI 가 쓴 로그도 사후 창작 가능.  검증 도구도 오염된다.
"10개마다 저장"  →  ★ 세는 것 자체가 누적 능력이다 → 순환.
                    게다가 큰 모델은 그냥 세버려서 구조 검증을 가린다.
"적어봐"        →  못 적으면 = 안 함 (확실)
                    적으면  = 함 or 그 순간 만듦 (★ 구별 불가)
```

**실제 사례:** 버퍼가 사건 요약처럼 5줄만 나와서 지적하자
AI 가 **"맞습니다. 매 턴 버퍼를 새로 쓰고 있었습니다"** 라고 시인했다.
→ **AI 는 append(누적)를 진짜로는 못 한다. 보여줄 때마다 새로 그릴 뿐이다.**
→ 규칙(매 턴 append + 턴 수 명시 + 10개 flush)을 주자 그제야 실제로 누적됐다.

```
AI 안의 상태:   관측해야 확정 → 안 보면 중첩          (양자)
코드의 상태:    코드가 상시 확정 → 안 봐도 정해져 있음  (고전)
   → ★ 유일한 탈출은 상태를 AI 밖(코드 변수)으로 빼는 것
   → 코드는 창작을 못 한다.  변수에 진짜 10개가 있어야 10개를 쓴다.
```

**실용적 절충 (코드 전까지):** 가짜 반증은 싸고 진짜 증명은 비싸다.
안 돌면 개수·내용이 몇 번 만에 어긋난다(실제로 한 번에 들통났다).
그러니 **NOW 는 화면에서 숨기더라도 10턴마다 파일로 저장한다** — 그 파일이 검증 창구다.

### 아직 모르는 것

```
??  knows 추적을 AI 가 정확히 하나  ← ★ 실전 버그로 발견 (§규칙 3-3)
??  확률 분포가 정확한가            ← 여러 번 발동은 확인.  분포는 미확인.
                                      편향이면 난수를 코드로.
??  캐릭터가 변해도 되나.  얼마나 자주.  (§Q5)
??  코산에 없는 발화가 왔을 때.  STOP? 폴백? (§발견 2)
??  매칭을 로컬이 할 수 있나.  객관식이면 될 것 같은데 재봐야 안다.
??  wound 가 다 드러나면 그 캐릭터는 끝인가
??  캐릭터 100명이면 NOW 가 커진다.  누구를 NOW 에 올리나
    → ★ 임시:  지금 대화 중인 사람 + 언급된 사람만
    → 조우(§Q7)로 일부 해결:  본 사람은 knows 에만, 말 건 사람만 NOW 에
??  사용자도 C_* 인가.  사용자의 principle 은 누가 정하나
??  turn 당 실제 비용이 로어북보다 싼가 비싼가  ← 재봐야 안다
??  voice 를 한 줄로 줄이면 말투가 밋밋해지나
??  BG 를 매 턴 강제할지, 가끔 할지 (조용한 실내 대화에서 억지스러워지는지)
```

**채우지 않는다. 굴리다 막히면 그 자리가 답을 준다.**
**그리고 실전에서 새는 자리가 다음 규율을 준다 — knows 버그가 §규칙 3-3 을 낳았듯.**

---

## 세션 시작 — 정의를 강제로 읽혀야 한다 ★★ 실전 발견

**정의를 안 읽으면 규율이 통째로 꺼진다.** def/*.cosan 은 자기설명적이라
정의 없이도 "구조"는 파악되지만, **"규율"(NOW 유지, knows 사용법, 누적)은
정의에만 있다.** 안 읽으면 대충 작동하고, 그래서 빠진 것을 모른다.

```
대조 실험:
   정의 읽은 세션    →  NOW · knows · open · 모드 다 켜짐
   정의 안 읽은 세션  →  그냥 대화.  NOW 없음.
   → ★ 정의 읽기가 규율의 on/off 스위치다
```

**그리고 "필수 파일 목록"으로는 AI 가 읽지 않는다.**

```
✗  "시작 시 필수 파일:  채팅용_코산.md,  def/INDEX.cosan"
   → AI 는 "아 이게 필수구나" 하고 인지만 하고 넘어간다 (실제로 그랬다)

✓  ## 세션 시작 — 반드시 이 순서로 실행 (건너뛰면 규율이 새서 오작동)
   STEP 1.  지금 `채팅용_코산.md` 를 read 한다.
            → 안 읽으면 NOW 를 유지·출력하지 않고 knows 규율을 어긴다.
   STEP 2.  지금 `def/INDEX.cosan` 을 read 한다.
   STEP 3.  INDEX 를 보고 필요한 def/ 파일을 read 한다.
   ★ STEP 1~3 완료 전에는 대화를 시작하지 마라.
   ★ "읽을 파일이 이거구나" 하고 목록만 확인하는 것은 위반이다.  실제로 read 하라.
```

**서술이 아니라 명령형 절차 + 이유 + 검증이어야 실행된다.**
이 블록은 프로젝트 진입점(PROJECT_MAP)에 둔다.

**버전 앵커링도 주의한다.** 파일명만 주면 이미 아는 옛 버전으로 답한다
(v4 를 주었는데 v1 기억으로 답한 사례). **"알고 있다고 여기지 마라 — 이전 버전과
다르다"** 를 함께 적어야 실제로 읽는다.
